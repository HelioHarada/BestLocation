/*

    Promise: objeto representa a conclusão final (ou falha) de uma operação assíncrona e seu valor resultante

    body-parser: quem trata as requisições e disponibiliza o objeto "req.body"
        "req.body": para facilitar o trabalho com os dados enviados para as rotas

    consign: faz o autoload dos scripts e injeta na instância

    Controller (Express): todo controller é uma função e recebe dois parâmetros obrigatórios (req, res)
        "(req)": representa a requisição
        "(res)": representa a resposta 
            --> Ambos são objetos que podem ser manipulados facilmente
*/